enum EPlatform {
    IOS
    ANDROID
    MACOS
    WINDOWS
    LINUX
}

enum ECallStatus {
    ANSWERED,
    NOT_ANSWERED,
    REJECTED
}

enum EStatus {
    COMPLETED,
    REJECTED,
    CANCELLED,
    FAILED,
    ACTIVE,
    INACTIVE
}

enum EFAQCategory {
    SYSTEM,
    APPLICATION,
    TECHNICAL,
    INTERFACE,
    CLAIMS,
    SUPPORT
}

enum ERole {
    ROLE_ADMIN,
    ROLE_USER,
    ROLE_TRANSLATOR
}